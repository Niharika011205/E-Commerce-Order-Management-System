name: Backend CI / build-and-test (Auth-Service/Auth-Service)

on:
  push:
    branches: [ main ]
    paths:
      - 'Auth-Service/**'
      - '.github/workflows/auth-service.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'Auth-Service/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Set up JDK 21
      uses: actions/setup-java@v3
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Build with Maven
      run: cd Auth-Service/Auth-Service && mvn clean package -DskipTests

    - name: Run tests
      run: cd Auth-Service/Auth-Service && mvn test
